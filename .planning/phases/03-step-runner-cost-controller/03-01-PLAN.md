---
phase: phase-3
plan: 01

# Plan 03-01: Step Runner Core + Cost Controller

## Objective
Implement the core step execution primitive (runStep()) with budget enforcement, cost tracking, verification callbacks, error handling, and failure cascade.

## Requirements Covered
- STEP-01 through STEP-06: Step runner with budget, verification, error handling
- COST-01 through COST-04: Cost controller with per-step and per-phase tracking

## Tasks
1. Define step runner types (StepOptions, StepResult, StepContext, CascadeOptions)
2. Implement cost controller (checkBudget, recordStepCost, getPhaseTotal)
3. Implement runStep() wrapping executeQuery()
4. Implement runStepWithCascade() with retry logic
5. Unit tests for step runner, cascade, and cost controller
6. Integration and scenario tests
